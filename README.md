# Big Two — Server

Node.js Socket.IO server for the Big Two card game.

## Commands

```bash
npm run devStart  # Dev with nodemon on port 5000
```

## Stack

Socket.IO, no framework. Runs in a Docker container (no public port — proxied via client container Nginx).
